#include <stdio.h>
#include <sys/shm.h>
#include <sys/ipc.h> //not sure about it 
#include <sys/types.h> //not sure about it either 
#include <sys/mman.h>
//ma wpisac do shared memory "pamięć współdzielona 123"
/*
shmget(), 
shmat(), 
shmdt(), 
shmctl(), 
ftok(), 
fork(), 
wait(), 
Struktura shmid_ds{} i jej struktury składowe. 
*/
int main()
{
    int shmget()
    return 0;
}